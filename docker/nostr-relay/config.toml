[info]
name = "RéseauRacine Relay (dev)"
relay_url = "ws://localhost:8080"
description = "Relais Nostr local pour le développement RéseauRacine"
pubkey = ""
contact = ""

[options]
reject_future_seconds = 1800
remote_ip_header = "x-forwarded-for"

[limits]
messages_per_sec = 100
blob_size_limit = 100000
event_size_limit = 100000
max_filters = 50
max_ subscriptions = 50
max_events_per_connection = 10000
